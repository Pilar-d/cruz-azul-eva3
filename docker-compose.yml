version: '3.8'

services:
  app:
    build:
      context: ./app
    container_name: cruz_azul_app
    ports:
      - "3000:3000"
    environment:
      - DB_HOST=database-cruzazul.ciihvjvbtoq2.us-east-1.rds.amazonaws.com
      - DB_USER=postgres_admin
      - DB_PASSWORD=database-cruzAzul123
      - DB_NAME=databaseCruzAzul
      - DB_PORT=5432
